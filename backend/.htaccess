RewriteEngine On

# Ak súbor alebo priečinok existuje, servíruj ho priamo
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Všetko ostatné presmeruj na index.php
RewriteRule ^(.*)$ index.php [QSA,L]
